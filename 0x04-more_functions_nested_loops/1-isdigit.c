#include "main.h"

/**
* _isdigit - Check if a character is a digit
* @x: The number varaible to test
*
* Return: 1 for digit or 0 otherwise
*/
int _isdigit(int x)
{
if (x >= 48 && x <= 57)
{
return (1);
}

return (0);
}

